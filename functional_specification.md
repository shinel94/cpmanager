# Chord Progression Manager 세부 기능 정의서

## 1. 문서 개요

- 문서 목적: MVP 개발을 위한 기능·데이터·처리 규칙 정의
- 대상 시스템: Next.js App Router 기반 Node.js 로컬 웹 애플리케이션
- Client UI: React Server Component 및 Client Component
- Backend: Next.js Route Handler
- 저장소: SQLite
- 인증: 없음
- 네트워크 서버: 외부 서버 없음
- 박자: 4/4 고정
- 조성: 12개 표준 장조 선택만 지원
- 편집 상태: 클라이언트 초안
- 프로젝트 DB 반영: 명시적 전체 저장 시에만 수행

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 프로젝트 | 사용자가 작성하는 한 곡의 전체 작업 단위 |
| 송폼 | 곡을 구성하는 구간의 순서와 구간별 마디 수 |
| 구간 | Intro, Verse, Chorus 등 곡의 구성 단위 |
| 도수 | 조성 안에서 코드의 기능을 나타내는 대문자 로마 숫자 값 |
| 코드 속성 | 화음 유형, 텐션, 베이스 등 도수 외의 코드 정보 |
| 추천 블록 | 4마디·4코드로 구성된 추천 단위 |
| 사용자 진행 | 사용자가 직접 저장한 4마디 도수 진행 |
| 기법 | 코드 변경 관계를 설명하는 음악 이론적 분류 |

## 3. 기능 우선순위

- P0: MVP 필수
- P1: MVP 보완
- P2: 후속 개발

## 4. 프로젝트 기능

### FR-PROJ-001 새 프로젝트 생성

- 우선순위: P0
- 사용자는 새 프로젝트를 생성할 수 있어야 한다.
- 프로젝트 생성 시 곡 이름을 입력할 수 있어야 한다.
- 조성은 장조 중 하나를 선택해야 한다.
- 기본 송폼 구간은 비어 있거나 최소 구간으로 시작할 수 있다.

### FR-PROJ-002 프로젝트 이름 변경

- 우선순위: P0
- 사용자는 프로젝트 이름을 변경할 수 있어야 한다.
- 이름은 공백만으로 구성될 수 없다.

### FR-PROJ-003 프로젝트 저장

- 우선순위: P0
- 사용자가 저장 동작을 실행했을 때 프로젝트 정보를 SQLite에 저장한다.
- 저장 대상은 프로젝트 메타데이터, 조성, 송폼, 마디, 코드 입력값이다.
- 자동 저장은 제공하지 않는다.
- 송폼·코드·추천 적용 결과는 클라이언트 초안으로 유지하며, 명시적 전체 저장 요청에서만 SQLite에 반영한다.
- 저장 성공과 실패를 사용자에게 표시한다.

### FR-PROJ-004 프로젝트 목록

- 우선순위: P0
- 저장된 프로젝트의 이름 목록을 표시한다.
- 목록에서 프로젝트를 선택하면 불러오기 또는 삭제를 실행할 수 있어야 한다.

### FR-PROJ-005 프로젝트 불러오기

- 우선순위: P0
- 사용자가 프로젝트를 선택하면 SQLite의 저장 데이터를 화면에 복원한다.
- 송폼 순서, 조성, 마디 수, 코드 속성을 저장 당시 상태로 복원한다.

### FR-PROJ-006 프로젝트 삭제

- 우선순위: P0
- 삭제 전 확인을 요구한다.
- 프로젝트 삭제 시 하위 송폼·마디·코드 데이터도 함께 삭제한다.

## 5. 송폼 기능

### FR-FORM-001 구간 추가

- 우선순위: P0
- 기본 구간명 목록을 제공한다.
- 사용자가 직접 구간명을 입력할 수 있어야 한다.
- 기본 구간명: Intro, Verse, Pre-Chorus, Chorus, Interlude, Bridge, Outro

### FR-FORM-002 구간 순서 관리

- 우선순위: P0
- 구간의 순서에는 제한을 두지 않는다.
- 사용자는 구간을 추가한 순서와 관계없이 순서를 변경할 수 있어야 한다.

### FR-FORM-003 구간 마디 수 설정

- 우선순위: P0
- 구간별 마디 수는 1 이상의 정수로 설정한다.
- 마디 수는 송폼 표시와 추천 블록 생성에 사용한다.

### FR-FORM-004 구간 삭제

- 우선순위: P0
- 구간 삭제 전 확인을 요구한다.
- 구간에 포함된 마디와 코드 입력값도 삭제한다.

## 6. 조성 및 코드 기능

### FR-CODE-001 조성 선택

- 우선순위: P0
- 장조 조성을 선택할 수 있어야 한다.
- 예: C Major, D Major, E Major
- 선택한 조성은 도수를 실제 코드로 변환하는 기준으로 사용한다.
- 단조를 독립 조성으로 선택하는 기능은 MVP에서 제외한다.

### FR-CODE-002 다이어토닉 코드 표시

- 우선순위: P0
- 선택한 장조의 기본 다이어토닉 코드를 표시한다.
- C Major 예시: C, Dm, Em, F, G, Am, Bdim
- 사용자가 특정 도수를 선택하면 코드 속성 편집 영역을 표시한다.

### FR-CODE-003 코드 속성 선택

- 우선순위: P0
- 다음 속성을 선택할 수 있어야 한다.
- 장조, 단조, 디미니시드
- maj7, 7, m7
- 9
- 대표적인 모달 인터체인지
- 세컨더리 도미넌트
- 슬래시 코드
- 화면에는 전형적인 코드 변형을 우선 표시한다.

### FR-CODE-004 코드 차트 입력

- 우선순위: P0
- 기본 입력 단위는 1마디 1코드이다.
- 사용자는 1마디의 1~4박에 코드를 입력할 수 있다.
- 코드가 입력되지 않은 위치는 빈 값으로 유지할 수 있다.
- 코드 입력 시 도수와 코드 속성을 함께 저장한다.

### FR-CODE-005 실제 코드 변환

- 우선순위: P0
- 내부 도수와 코드 속성을 선택한 조성의 실제 코드명으로 변환한다.
- 조성이 변경되면 표시 코드를 다시 계산한다.
- 저장 데이터의 도수와 코드 속성은 유지한다.

## 7. 추천 기능

### FR-REC-001 추천 블록 생성

- 우선순위: P0
- 추천 블록은 정확히 4마디로 생성한다.
- 각 마디에 코드가 1개인 경우에만 기본 추천 대상으로 한다.
- 4마디 블록 안에 여러 코드가 있는 마디가 하나라도 있으면 해당 블록 전체를 추천에서 제외한다.

### FR-REC-002 추천 블록 범위 계산

- 우선순위: P0
- 구간의 시작부터 4마디씩 겹치지 않게 나눈다.
- 8마디는 1~4마디, 5~8마디로 나눈다.
- 7마디는 1~4마디만 추천하고 5~7마디는 제외한다.
- 3~6마디와 같은 겹치는 창은 만들지 않는다.
- 4마디 미만 구간은 추천하지 않는다.

### FR-REC-003 입력 조건 반영

- 우선순위: P0
- 사용자가 입력한 대문자 도수 또는 코드 속성과 일치하는 진행만 결과에 포함한다.
- 기존 코드 입력값은 추천 적용 시 유지한다.
- 4개 위치가 모두 입력된 경우 추천하지 않는다.
- 조건에 맞는 진행이 없으면 `추천 없음`을 표시한다.
- 복수 코드 마디가 포함된 블록은 `multi_chord_excluded` 상태로 표시한다.

### FR-REC-004 추천 정렬 기준

- 우선순위: P0
- 다음 기준을 제공한다.
- 무작위
- 대중성 우선
- 코드 연결성 우선
- 다양성 우선
- 대중성·연결성·다양성 값은 추천 DB에 저장된 내부 기준을 사용한다.
- 무작위 기준은 조건에 맞는 진행 중에서 무작위로 선택한다.

### FR-REC-005 추천 결과 표시

- 우선순위: P0
- 기본적으로 3개의 추천 결과를 표시한다.
- UI 크기 또는 결과 수에 따라 표시 수는 조정할 수 있다.
- 다음 페이지에서 추가 추천 결과를 확인할 수 있어야 한다.
- 각 결과에는 도수 진행과 실제 코드 진행을 함께 표시한다.
- 사용자는 추천 결과를 현재 4마디 블록에 적용할 수 있어야 한다.

## 8. 기법 분석 기능

### FR-ANA-001 분석 범위

- 우선순위: P0
- 분석 범위는 최대 4마디이다.
- 4마디 블록의 마지막 코드와 다음 4마디 블록의 첫 코드 사이에는 분석하지 않는다.
- 분석에 필요한 이전·다음 코드가 4마디 범위를 벗어나면 사용하지 않는다.

### FR-ANA-002 복수 코드 마디 처리

- 우선순위: P0
- 여러 코드가 있는 마디 자체는 별도 진행 분석에서 제외한다.
- 이전 마디와의 관계를 분석할 때는 해당 마디의 첫 코드를 사용한다.
- 다음 마디와의 관계를 분석할 때는 해당 마디의 마지막 코드를 사용한다.
- 마디 중간에 있는 코드는 이 분석에 사용하지 않는다.

### FR-ANA-003 기법 판별

- 우선순위: P0
- 코드 변경 전후의 도수와 코드 속성을 규칙 DB와 비교한다.
- 다음 기법을 포함한다.
- 모달 인터체인지
- 세컨더리 도미넌트
- 코드 변형 및 텐션 관련 규칙
- 슬래시 코드 관련 규칙
- 조건에 맞는 기법이 없으면 기법 안내를 표시하지 않는다.

### FR-ANA-004 중복 기법 처리

- 우선순위: P0
- 여러 규칙이 일치하면 priority가 가장 높은 규칙 하나만 표시한다.
- priority는 기법 규칙 데이터에 저장한다.
- 같은 priority인 경우 고정된 규칙 ID 순서를 사용한다.

## 9. 사용자 진행 기능

### FR-USER-001 사용자 진행 등록

- 우선순위: P0
- 사용자 진행은 정확히 4개 도수 위치로 저장한다.
- 입력 항목은 다음과 같다.
- 진행 이름
- 4개 도수 또는 도수 토큰
- 자주 사용된 송폼 복수 선택
- 설명 또는 메모
- 등록일은 사용자가 입력하지 않고 시스템이 관리한다.

### FR-USER-002 사용자 진행 검색

- 우선순위: P0
- 검색은 실제 코드명이 아니라 도수 패턴을 기준으로 한다.
- 검색값은 정확히 4개 위치를 가진다.
- `x`는 해당 위치의 임의 도수를 의미한다.
- `x`는 모든 위치에 사용할 수 있다.
- 모든 위치가 `x`이면 검색을 실행하지 않는다.
- 결과는 검색 조건을 충족하는 4마디 진행만 표시한다.

예시:

```text
저장: I - II - I - I
x - II - I - x  -> 일치
I - II - x - x  -> 일치
x - x - I - I   -> 일치
I - I - x - x    -> 불일치
x - x - x - x   -> 검색하지 않음
```

### FR-USER-003 사용자 진행 분리

- 우선순위: P0
- 사용자가 등록한 진행은 기본 추천 결과에 포함하지 않는다.
- 사용자 진행 관리 화면에서만 검색·조회한다.

### FR-USER-004 사용자 진행 관리

- 우선순위: P0
- 사용자 진행 목록 보기
- 이름 검색
- 도수 패턴 검색
- 상세 내용 보기
- 삭제
- 사용자 진행은 SQLite에 저장한다.

## 10. SQLite 데이터 정의

### projects

| 필드 | 설명 |
|---|---|
| id | 프로젝트 식별자 |
| name | 곡 이름 |
| tonic | 기준 조성의 으뜸음 |
| mode | 초기값 major |
| created_at | 생성 시각 |
| updated_at | 저장 시각 |

### sections

| 필드 | 설명 |
|---|---|
| id | 구간 식별자 |
| project_id | 프로젝트 ID |
| position | 송폼 순서 |
| name | 구간명 |
| bar_count | 구간 마디 수 |

### bars

| 필드 | 설명 |
|---|---|
| id | 마디 식별자 |
| section_id | 구간 ID |
| position | 구간 안의 마디 순서 |

### bar_chords

| 필드 | 설명 |
|---|---|
| id | 코드 입력 식별자 |
| bar_id | 마디 ID |
| beat | 1~4박 |
| degree | 대문자 기능 도수 (I, II, V, bVI 등) |
| quality | major, minor, diminished 등 |
| extension | maj7, 7, m7, 9 등 |
| bass_degree | 대문자 슬래시 베이스 도수 (예: VII) |

### system_recommendation_progressions

| 필드 | 설명 |
|---|---|
| id | 추천 진행 식별자 |
| name | 진행 이름 |
| description | 설명 |
| popularity_score | 대중성 기준 |
| connectivity_score | 연결성 기준 |
| diversity_group | 다양성 그룹 |
| priority | 추천·규칙 우선순위 |
| created_at | 시스템 관리 시각 |

### system_progression_form_tags

| 필드 | 설명 |
|---|---|
| progression_id | 기본 추천 진행 ID |
| form_tag | Intro, Verse, Pre-Chorus 등 |

기본 키는 `(progression_id, form_tag)`로 한다.

### system_progression_steps

| 필드 | 설명 |
|---|---|
| id | 진행 단계 식별자 |
| progression_id | 기본 추천 진행 ID |
| position | 1~4 |
| degree | 대문자 기능 도수 |
| quality | 화음 유형 |
| extension | 코드 확장 |
| bass_degree | 베이스 도수 |

### user_progressions

| 필드 | 설명 |
|---|---|
| id | 사용자 진행 식별자 |
| name | 진행 이름 |
| description | 설명 또는 메모 |
| created_at | 시스템 관리 시각 |

### user_progression_form_tags

| 필드 | 설명 |
|---|---|
| progression_id | 사용자 진행 ID |
| form_tag | 사용자가 선택한 송폼 |

기본 키는 `(progression_id, form_tag)`로 한다.

### user_progression_steps

| 필드 | 설명 |
|---|---|
| id | 진행 단계 식별자 |
| progression_id | 사용자 진행 ID |
| position | 1~4 |
| degree | 대문자 기능 도수 |
| quality | 화음 유형 |
| extension | 코드 확장 |
| bass_degree | 베이스 도수 |

### technique_rules

| 필드 | 설명 |
|---|---|
| id | 규칙 식별자 |
| name | 기법명 |
| rule_type | 기법 유형 |
| condition | 기법 유형별 구조화 JSON 판별 조건 |
| description | 사용자 표시 설명 |
| priority | 중복 시 우선순위 |
| enabled | 사용 여부 |

## 11. 입력 검증

- 프로젝트 이름은 필수이며 공백만 입력할 수 없다.
- 조성은 지원 목록에 포함된 장조만 허용한다.
- 구간명은 필수이다.
- 마디 수는 1 이상의 정수여야 한다.
- 박자는 1~4 범위의 정수여야 한다.
- 추천 진행은 정확히 4개의 단계가 있어야 한다.
- 사용자 진행 검색은 정확히 4개의 검색 토큰이 있어야 한다.
- 모든 검색 토큰이 `x`이면 검색하지 않는다.
- 코드 속성은 지원 목록에 포함된 값만 저장한다.

## 12. 주요 처리 흐름

### 추천 처리

1. 사용자가 송폼 구간과 4마디 블록을 선택한다.
2. 시스템이 해당 블록의 마디별 코드 개수를 확인한다.
3. 추천 대상에서 제외되는 마디와 이미 입력된 위치를 확인한다.
4. 조성과 입력 조건을 도수 기준으로 정규화한다.
5. 기본 추천 데이터에서 조건에 맞는 진행을 검색한다.
6. 선택한 정렬 기준으로 결과를 정렬 또는 무작위 선택한다.
7. 최대 3개의 결과를 화면에 표시한다.
8. 사용자가 결과를 적용하면 도수와 코드 속성을 프로젝트에 저장한다.
9. 선택한 조성의 실제 코드명을 화면에 표시한다.

### 기법 분석 처리

1. 사용자가 코드 또는 코드 속성을 변경한다.
2. 분석 대상 4마디 범위를 결정한다.
3. 복수 코드 마디의 첫 코드·마지막 코드 예외 규칙을 적용한다.
4. 변경 전후 코드와 인접 진행을 도수·속성으로 정규화한다.
5. 활성화된 기법 규칙을 priority 순으로 검사한다.
6. 가장 우선순위가 높은 일치 기법 하나를 표시한다.

### 사용자 진행 검색 처리

1. 사용자가 4개의 도수 토큰을 입력한다.
2. 모든 토큰이 `x`인지 확인한다.
3. 사용자 진행 데이터만 검색한다.
4. 각 위치의 고정 토큰만 비교한다.
5. 네 위치 모두 조건을 통과한 진행만 반환한다.

## 13. 완료 조건

### MVP 완료 조건

- 사용자가 새 프로젝트를 생성할 수 있다.
- 송폼 구간과 마디 수를 설정할 수 있다.
- 장조 조성을 선택할 수 있다.
- 코드 차트에 코드를 저장할 수 있다.
- 4마디·4코드 추천을 받을 수 있다.
- 추천 도수를 실제 코드로 확인할 수 있다.
- 코드 변경 기법을 확인할 수 있다.
- 프로젝트를 수동 저장·불러오기·삭제할 수 있다.
- 사용자 진행을 저장하고 4개 도수와 와일드카드로 검색할 수 있다.
- Node.js 기반 로컬 환경에서 전체 흐름이 실행된다.

### 후속 기능 완료 조건

- BPM 설정이 가능하다.
- 코드 진행을 기본 MIDI 데이터로 재생할 수 있다.
- Node.js 없이 설치형 앱으로 실행할 수 있다.

## 14. 명시적 제외 범위

- 온라인 계정 및 인증
- 외부 서버 저장
- PDF·이미지·MIDI 파일 내보내기
- 자동 저장
- 독립적인 단조 조성 선택
- 4마디 블록을 넘어서는 기법 분석
- 여러 코드가 있는 마디 자체의 추천·기법 분석
