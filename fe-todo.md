# Chord Progression Manager Client-side (FE) TODO

개발 기준 문서:

- `development_plan.md`
- `functional_specification.md`
- `product_plan.md`
- `progression/README.md`
- `todo.md` (Backend API 및 도메인 규칙 연계)

## 개발 원칙

- Next.js App Router, React 19 Client Component, TypeScript를 기준으로 개발한다.
- 애플리케이션 코드는 프로젝트 루트의 `app/` 아래에 배치하며, 프론트엔드 컴포넌트는 `app/components/`, 클라이언트 훅 및 유틸은 `app/lib/client/`로 구조화한다.
- **클라이언트 초안(Draft) 패턴**:
  - 송폼 추가·수정·삭제, 마디 수 변경, 코드 입력·수정, 추천 결과 적용은 모두 React 로컬 상태(초안)로 관리한다.
  - 명시적인 전체 저장 버튼 클릭 시에만 `PUT /api/projects/:id` 트랜잭션을 호출하여 SQLite에 일괄 반영한다 (자동 저장 없음).
  - 저장되지 않은 변경사항이 있는 경우 화면 이탈(페이지 이동, 프로젝트 전환) 시 확인 경고를 띄운다(Dirty check).
- **데이터와 표시의 엄격한 분리**:
  - 내부 상태는 대문자 로마 숫자 도수(`degree`)와 유한 코드 속성(`quality`, `extension`, `bass_degree`)으로 보존한다.
  - 화면에는 현재 선택된 12개 장조 조성(`tonic`)에 맞춰 순수 변환 함수 또는 Meta API를 통해 계산된 실제 코드명(`displayName`)을 표시한다.
- **4마디·4코드 블록 중심 UI/UX**:
  - 추천 및 기법 분석은 겹치지 않는 4마디 단위 블록으로 시각화한다.
  - 1마디 복수 코드 마디가 포함된 블록은 `multi_chord_excluded` 상태를 안내하고 추천을 차단한다.
  - 4칸이 모두 채워진 블록은 추천 버튼을 비활성화하고 전체 입력 완료 상태를 안내한다.
  - 추천 적용 시 사용자가 이미 입력해 둔 위치는 유지하고 빈 마디만 비파괴적으로 병합(Merge)한다.
- **실시간 기법 분석 피드백**:
  - 사용자가 코드 또는 속성을 변경했을 때 저장된 프로젝트 ID에 의존하지 않고 현재 초안의 4마디 컨텍스트를 서버 API로 전달하여 적용된 이론 기법(모달 인터체인지, 세컨더리 도미넌트 등)을 배지 및 설명으로 표시한다.
  - 시스템 기본 추천 160선과 사용자가 직접 등록한 진행은 UI 상 완전히 분리된 영역(별도 탭/화면)에서 제공한다.
- **초안(Draft) 기반 추천·분석 통신 원칙**:
  - 추천(`POST /api/recommendations`) 및 기법 분석(`POST /api/analysis`) 호출 시, 미저장 상태의 변경사항이 반영될 수 있도록 클라이언트 초안의 조성·송폼·마디/코드 컨텍스트를 페이로드에 전달하여 DB 미반영에 따른 불일치를 방지한다.
- **순수 도메인 로직 공유**:
  - 도수→실제코드 변환(`chord-realizer.ts`), 4마디 블록 분할(`recommendation-blocks.ts`), 카탈로그(`chord-catalog.ts`)는 `app/lib/shared/` 모듈을 통해 클라이언트와 서버가 직접 공유한다.
- **입문자 10분 완성 지향**:
  - 송폼 선택 → 다이어토닉 코드/추천 카드 클릭 → 저장으로 이어지는 간결하고 직관적인 워크플로우를 보장한다.

## 병렬 작업 운영 규칙

- `[P]`는 같은 Wave 내에서 서로 다른 컴포넌트나 모듈을 맡아 독립적으로 진행할 수 있는 작업이다.
- `[G]`는 다음 Wave로 진입하기 위해 반드시 충족해야 하는 Gate 조건이다.
- 클라이언트-서버 간 API 명세(`todo.md`의 Phase 6~11 API 계약)가 고정된 후에는 Mock 데이터를 활용하여 백엔드 완성 전에도 병렬 개발할 수 있다.
- 공통 상태 스토어 인터페이스, 공통 타입 선언(`app/types/`)이 변경될 경우 관련 병렬 작업을 일시 중단하고 타입을 먼저 동기화한다.

## 병렬 실행 Wave

### Wave 0. 프론트엔드 개발 환경 및 UI 셸

선행 조건: Next.js 실행 환경 및 레이아웃 (`todo.md` G0)

- [x] UI 스타일링 시스템 및 전역 CSS/레이아웃 뼈대 구축 (Tailwind CSS v4)
- [x] 공통 헤더 (프로젝트 타이틀, 현재 곡명, 저장 상태 표시, 전체 저장 버튼)
- [x] 3단 워크스페이스 레이아웃 (좌측: 송폼·조성, 중앙: 코드 차트, 우측: 추천·기법 패널)
- [x] 모달, 다이얼로그, 토스트 알림 공통 컴포넌트 작성
- [x] 클라이언트 API HTTP 호출 래퍼 (`apiClient`) 및 공통 에러 처리기 작성

`[G0-FE]` Next.js 로컬 서버에서 3단 워크스페이스 셸 레이아웃과 모달/토스트가 정상 렌더링되고, 기본 반응형 배치가 깨지지 않아야 한다.

### Wave 1. 클라이언트 도메인 타입 및 초안 상태 스토어

선행 조건: `[G0-FE]` 및 백엔드 도메인 카탈로그 계약 (`todo.md` G1)

- [x] 프론트엔드 TypeScript 도메인 모델 정의 (`ProjectDraft`, `Section`, `Bar`, `Chord`, `RecommendationItem` 등)
- [x] 프로젝트 초안 상태 관리 컨텍스트/스토어 (`ProjectDraftContext` / custom hooks)
- [x] 클라이언트 측 도수 → 코드명 실시간 파생 변환 유틸리티 (`realizeChord`)
- [x] 초안 변경 감지(`isDirty`) 및 롤백/복원 로직 구현

`[G1-FE]` 메모리 상에서 프로젝트 생성, 송폼 추가, 마디 코드 입력, 조성 변경에 따른 코드명 재계산이 단위 테스트 및 상태 검증으로 통과해야 한다.

### Wave 2. 프로젝트 관리 UI & 조성 선택

선행 조건: `[G1-FE]` 및 백엔드 프로젝트 API (`todo.md` G4)

- [x] `[P]` 프로젝트 목록 모달/사이드바 (목록 조회, 곡 검색, 프로젝트 선택, 삭제 확인)
- [x] `[P]` 새 프로젝트 생성 모달 (곡 이름 입력 검증, 12개 표준 장조 선택)
- [x] `[P]` 프로젝트 헤더 인라인 곡명 수정 및 명시적 저장(`PUT /api/projects/:id`) 연동
- [x] `[P]` 상단 12개 장조 조성 선택기 (`C`, `Db`, `D`, `Eb`, `E`, `F`, `Gb`, `G`, `Ab`, `A`, `Bb`, `B`)

`[G2-FE]` 신규 프로젝트 생성 → 조성 변경 → 곡명 수정 → 수동 저장이 실제 API 호출 및 UI 피드백(저장 완료 토스트)과 함께 정상 동작해야 한다.

### Wave 3. 송폼 편집기 & 코드 차트 그리드

선행 조건: `[G2-FE]`

- [x] `[P]` 송폼 구간 목록 컴포넌트 (기본 프리셋 7종 선택 및 직접 입력, 드래그 앤 드롭 및 상/하 버튼 순서 변경, 삭제)
- [x] `[P]` 구간별 마디 수(1 이상 정수) 증감 컨트롤러
- [x] `[P]` 4/4 박자 기반 코드 차트 메인 그리드 (마디 번호 표시, 1마디 1코드 뷰)
- [x] `[P]` 1마디 4박 분할 입력 뷰 (비트별 코드 배치 시각화)
- [x] `[P]` 빈 마디 및 복수 코드 마디 시각적 구분 처리
- [x] `[P]` 빈 마디는 항상 `chords: []`로 유지하고 코드 삭제 시 동일 형식으로 정규화

`[G3-FE]` 구간 추가·마디 수 변경 시 차트 그리드가 즉시 반응하고, 마디 수 증감에 따른 초안 데이터 정리가 로컬에서 올바르게 유지되어야 한다.

### Wave 4. 다이어토닉 코드 팔레트 & 코드 속성 편집 팝오버

선행 조건: `[G3-FE]`

- [x] `[P]` 선택한 장조의 다이어토닉 7코드 퀵 선택 바 (C Major: C, Dm, Em, F, G, Am, Bdim)
- [x] `[P]` 코드 상세 속성 팝오버/드로어:
  - 도수 선택 (I ~ VII 및 임시표 bVI, bVII 등)
  - 화음 유형 (quality: major, minor, diminished, dominant 등)
  - 텐션/확장 (extension: maj7, 7, m7, 9 등)
  - 베이스 도수 (bass_degree: 슬래시 코드 베이스 선택)
- [x] `[P]` 빠른 코드 변경 및 코드 삭제(빈 마디화) 액션

`[G4-FE]` 코드 차트의 특정 마디를 클릭하여 다이어토닉 코드를 즉시 입력하거나, 팝오버에서 텐션/슬래시 코드로 수정하면 차트에 올바른 실제 코드명이 표시되어야 한다.

### Wave 5. 4마디 추천 패널 & 기법 분석 피드백 UI

선행 조건: `[G4-FE]` 및 백엔드 추천/기법 API (`todo.md` G5)

- [x] `[P]` 송폼 4마디 블록 선택기 (8마디: 1~4, 5~8 / 7마디: 1~4만 허용 및 5~7 비활성화)
- [x] `[P]` 추천 상태 예외 화면 (복수 코드 마디 포함 안내, 4칸 모두 입력 완료 안내, 4마디 미만 안내)
- [x] `[P]` 추천 정렬 탭 (대중성 우선 / 코드 연결성 우선 / 다양성 우선 / 무작위)
- [x] `[P]` 추천 결과 카드 3개 뷰 (도수 진행, 현재 조성 실제 코드, 설명, 송폼 태그, 다양성 그룹)
- [x] `[P]` 추천 결과 페이지네이션 컨트롤러
- [x] `[P]` [추천 적용] 버튼 클릭 시 현재 4마디 블록 빈 칸 병합 로직
- [x] `[P]` 코드 변경 시 실시간 기법 분석 결과 카드/배지 렌더링 (`/api/analysis` 연동)

`[G5-FE]` 4마디 블록 선택 → 추천 조회 → 적용 시 기존 입력이 보존된 상태로 빈 칸이 채워지고, 코드 수정 시 적용된 기법이 즉시 UI에 피드백되어야 한다.

### Wave 6. 사용자 진행 관리 UI & 시나리오 통합 검증

선행 조건: `[G5-FE]` 및 백엔드 사용자 진행 API (`todo.md` G5)

- [x] `[P]` 사용자 진행 관리 전용 모달/페이지 (`UserProgressionsModal.tsx`)
- [x] `[P]` 사용자 진행 등록 폼 (진행 이름, 4개 도수 스텝, 송폼 복수 선택, 메모)
- [x] `[P]` 사용자 진행 4자리 도수 패턴 검색창 (`x` 와일드카드 지원, `x-x-x-x` 차단 안내)
- [x] `[P]` 사용자 진행 목록 조회, 이름 검색 및 개별 삭제 기능
- [x] `[P]` 시나리오 A(신규 작곡 10분 완성), 시나리오 B(사용자 진행 검색), 시나리오 C(코드 수정 및 기법 확인) E2E 검증

`[G6-FE]` 사용자 진행이 시스템 추천과 완전히 격리되어 등록·검색되고, 10분 내에 입문자가 한 곡을 완성하여 SQLite에 저장할 수 있는 전체 사용자 여정이 통과해야 한다.

---

## Wave 상태

- [x] Wave 0: 프론트엔드 개발 환경 및 UI 셸
- [x] Wave 1: 클라이언트 도메인 타입 및 초안 상태 스토어
- [x] Wave 2: 프로젝트 관리 UI & 조성 선택
- [x] Wave 3: 송폼 편집기 & 코드 차트 그리드
- [x] Wave 4: 다이어토닉 코드 팔레트 & 속성 편집기
- [x] Wave 5: 4마디 추천 패널 & 기법 분석 UI
- [x] Wave 6: 사용자 진행 관리 UI & 통합 검증

---

## Phase 0. 프론트엔드 UI 기반 및 셸 레이아웃

목표: 전체 애플리케이션의 화면 구조를 잡고 공통 UI 컴포넌트와 API 클라이언트를 준비한다.

- [x] Tailwind CSS v4 및 글로벌 스타일시트 설정 (`app/globals.css`, `postcss.config.mjs`)
- [x] 순수 도메인 모듈 `app/lib/shared/` 분리 및 서버/클라이언트 공유 (`chord-catalog.ts`, `chord-realizer.ts`, `recommendation-blocks.ts`)
- [x] 애플리케이션 메인 셸 레이아웃 구성:
  - 상단 글로벌 헤더: 로고, 프로젝트 이름 인라인 편집, 조성 표시, 저장 상태 배지(수정됨/저장됨), 저장 버튼
  - 3단 워크스페이스 레이아웃 (좌: 송폼/네비게이션, 중: 코드 차트 메인 작업영역, 우: 추천 및 분석 패널)
- [x] 공통 모달 및 다이얼로그 시스템 (`Modal`, `ConfirmDialog`, `Backdrop`)
- [x] 사용자 안내 및 에러 알림용 `Toast` 알림 시스템 (성공, 오류, 경고 메시지)
- [x] Fetch 기반 HTTP 클라이언트 래퍼 (`app/lib/client/api.ts`):
  - API 공통 에러 형식(`{ ok: false, error: { code, message } }`) 파싱
  - 네트워크 단절 및 서버 에러 핸들링
- [x] 페이지 이탈 방지 이벤트 리스너 (`beforeunload` 및 Next.js 라우팅 시 확인 팝업)

완료 기준:
- [x] 브라우저 접속 시 헤더와 3단 분할 레이아웃이 깔끔하게 표시된다.
- [x] 토스트 알림과 모달 열림/닫힘이 정상 작동한다.
- [x] API 에러 발생 시 사용자 친화적인 에러 메시지 팝업이 노출된다.

---

## Phase 1. 클라이언트 도메인 모델 및 초안(Draft) 상태 관리

목표: 서버 DB와 독립적인 프론트엔드 상태 머신과 로컬 편집 초안을 완성한다.

- [x] 클라이언트 TypeScript 타입 선언 (`app/types/client.ts`):
  - `ProjectDraft`, `SectionDraft`, `BarDraft`, `BarChordDraft`
  - `KeyTonic` (12개 장조: C, Db, D, Eb, E, F, Gb, G, Ab, A, Bb, B)
  - `RecommendationBlock`, `TechniqueFeedback`, `UserProgression`
- [x] `ProjectDraftContext` 및 `useProjectDraft` 커스텀 훅 구현 (`app/lib/client/project-draft-context.tsx`):
  - `currentProject`: 현재 편집 중인 전체 프로젝트 객체
  - `isDirty`: 서버 저장 데이터와 비교하여 변경 여부 판별
  - `selectedBlock`: 현재 선택된 4마디 추천 블록 식별자
  - `selectedChord`: 현재 편집 중인 마디 및 박자 선택 상태
  - `lastTechnique`: 최근 코드 변경에 따른 기법 분석 결과
- [x] 로컬 초안 조작 액션 리듀서/디스패처 (`app/lib/client/draft-reducer.ts`):
  - `UPDATE_PROJECT_META`: 이름, 조성 변경
  - `ADD_SECTION`, `REMOVE_SECTION`, `REORDER_SECTIONS`, `UPDATE_SECTION_BARS`
  - `SET_CHORD`, `CLEAR_CHORD`, `APPLY_RECOMMENDATION_BLOCK` (비파괴적 빈 마디 병합)
- [x] 클라이언트 사이드 코드명 파생(Derived) 계산:
  - 초안 상태(`ProjectDraft`)에는 순수 속성(`degree`, `quality`, `extension`, `bass_degree`)만 유지하고, 화면의 `displayName`은 `realizeChord(currentTonic, chord)`로 렌더링 시점에 실시간 파생 계산하여 stale display 방지
- [x] 전체 저장 payload 직렬화 유틸 (`app/lib/client/project-serializer.ts`):
  - 현재 초안 상태를 백엔드 `PUT /api/projects/:id` 스펙에 맞게 가공
  - `sections`의 `position`을 0부터 시작하는 순차 연속 정수(`0..N-1`)로 자동 재색인
  - 각 구간의 `bars`를 1부터 `bar_count`까지 빈틈없는 순차 연속 정수(`1..bar_count`)로 자동 재색인
  - 코드가 없는 빈 마디는 `chords: []`로 직렬화하여 백엔드 검증 통과
  - 서버 응답을 초안으로 변환하는 `hydrateProjectDraft` 구현

완료 기준:
- [x] 컴포넌트 간 리렌더링이 최적화된 상태로 프로젝트 초안의 모든 필드를 수정할 수 있다.
- [x] 변경 발생 시 `isDirty`가 true로 전환되고, 저장이 완료되면 false로 리셋된다.

---

## Phase 2. 프로젝트 관리 및 저장 UI

목표: 프로젝트 생성, 목록 조회, 불러오기, 전체 수동 저장을 구현한다.

- [x] 프로젝트 목록 모달 컴포넌트:
  - `GET /api/projects` 호출 및 목록 렌더링
  - 곡 이름 실시간 검색창 (`?q=`)
  - 프로젝트 항목 선택 시 불러오기 (`GET /api/projects/:id`) 및 초안에 적재
  - 프로젝트 삭제 버튼 및 삭제 재확인 컨펌 모달 (`DELETE /api/projects/:id`)
- [x] 새 프로젝트 생성 모달:
  - 곡 이름 입력 필드 (공백 불가 유효성 검증)
  - 기본 조성 선택 (12개 장조 라디오/셀렉트 박스)
  - `POST /api/projects` 호출 후 즉시 해당 프로젝트 편집 뷰로 전환
- [x] 상단 바 전체 저장 컨트롤:
  - [저장] 버튼: `isDirty`가 true일 때 활성화, 클릭 시 `PUT /api/projects/:id` 요청
  - 저장 중 로딩 스피너 및 저장 완료 성공 토스트 노출
  - 저장 실패 시 실패 원인 알림 및 초안 상태 유지
- [x] 프로젝트 이름 인라인 편집기 (헤더의 곡명을 클릭하여 직접 수정)
- [x] PUT 성공 응답의 최신 프로젝트 전체 트리로 초안 re-hydration 및 임시 ID 교체

완료 기준:
- [x] 새 프로젝트를 생성하고, 이름을 바꾸고, 수동으로 저장한 뒤 브라우저를 새로고침하여 불러와도 데이터가 정확히 복원된다.
- [x] 삭제 전 반드시 사용자 확인을 거친다.

---

## Phase 3. 조성 선택 및 송폼 편집 UI

목표: 12개 장조 조성 설정 및 곡의 송폼(구간과 마디 수)을 직관적으로 편집한다.

### 조성 선택 UI

- [x] 12개 표준 장조 선택 셀렉트/버튼 그룹 (`C`, `Db`, `D`, `Eb`, `E`, `F`, `Gb`, `G`, `Ab`, `A`, `Bb`, `B`)
- [x] 조성 변경 시 저장 데이터(도수/속성)는 유지하고 차트의 표시 코드명만 즉각 변환
- [x] 단조 곡을 쓰려는 사용자를 위해 "장조 기반 작곡 가이드" 툴팁 제공 (예: Am는 C Major 기준)

### 송폼 편집기 UI

- [x] 좌측 송폼 네비게이션 패널 컴포넌트 (`SectionList.tsx`)
- [x] 기본 제공 구간 프리셋 추가 버튼:
  - `Intro`, `Verse`, `Pre-Chorus`, `Chorus`, `Interlude`, `Bridge`, `Outro`
- [x] 사용자 정의 구간명 직접 입력 필드
- [x] 신규 빈 프로젝트 온보딩 뷰 (`sections.length === 0`일 때 시작 안내 가이드 및 빠른 시작 팝 송폼 프리셋 `[Intro(4) + Verse(8) + Chorus(8)]` 일괄 생성 버튼 제공)
- [x] 구간 목록 드래그 앤 드롭(HTML5 드래그 핸들 `⋮⋮`, 드래그/오버 시각 효과) 및 상/하 이동 버튼을 통한 순서 재배치 (`reorderSections`)
- [x] 구간별 마디 수 입력/증감 스텝퍼 (최소 1 이상의 정수)
- [x] 마디 수 감소 시 "초과된 마디의 코드가 삭제됩니다" 확인 안내
- [x] 구간 삭제 버튼 및 하위 마디/코드 동시 삭제 확인 모달

완료 기준:
- [x] 프리셋 및 직접 입력을 통해 송폼 구조를 자유롭게 구성할 수 있다.
- [x] 순서 변경 및 마디 수 증감이 초안 상태에 즉각 반영되며 코드 차트와 동기화된다.

---

## Phase 4. 코드 차트 그리드 & 마디 뷰어

목표: 송폼 구조에 맞춰 마디 그리드를 렌더링하고 박 단위 코드 입력을 시각화한다.

- [x] 구간별 코드 차트 그리드 레이아웃 (4마디 1줄 기본 그리드 시스템)
- [x] 마디 카드 컴포넌트 (`BarCard.tsx`):
  - 마디 번호(전체 마디 번호 및 구간 내 마디 번호) 표시
  - 4마디 추천 블록에 포함된 마디 시각적 그룹핑 가이드선
- [x] 2단계 마디 입력 인터랙션 (선택 후 할당):
  - 마디 클릭 시 코드가 자동 할당되거나 토글 삭제되지 않고, 해당 마디를 [선택(포커스)] 상태로 활성화 (인디고 링/배경 하이라이트)
  - 이미 코드가 있는 마디 클릭 시에도 우선 선택 상태로 전환되어 현재 코드 정보와 상세 수정/삭제 옵션 표시
  - 선택된 마디에 명시적인 [코드 삭제/비우기] 버튼 제공 (마디 단순 클릭 삭제 방지)
- [x] 1마디 1코드 뷰:
  - 마디 중앙에 큰 폰트로 실제 코드명(`displayName`) 렌더링
  - 보조 텍스트로 도수 및 코드 속성(`degree` + `quality`) 렌더링
- [x] 1마디 4박 분할 뷰:
  - 한 마디 안에 여러 코드가 있는 경우 1~4박 비트 눈금 표시
  - 각 박별 코드명 개별 렌더링 및 빈 박 처리
- [x] 마디/박 클릭 시 코드 편집 모드 활성화

완료 기준:
- [x] 송폼의 마디 수에 맞춰 차트 그리드가 깔끔하게 정렬된다.
- [x] 마디 클릭 시 즉시 코드가 주입되지 않고 선택(포커스) 상태로 전환된다.
- [x] 1마디 1코드와 1~4박 복수 코드 마디가 시각적으로 명확히 구별된다.

---

## Phase 5. 다이어토닉 코드 팔레트 & 코드 속성 편집기

목표: 입문자가 손쉽게 다이어토닉 코드를 입력하고 상세 텐션/속성을 변경할 수 있게 한다.

### 다이어토닉 코드 퀵 팔레트

- [x] 선택된 조성의 다이어토닉 7코드 버튼 바 상시 노출 (예: C Major의 경우 C, Dm, Em, F, G, Am, Bdim)
- [x] 선택 기반 코드 할당:
  - 사용자가 코드 차트에서 특정 마디를 선택한 상태에서 다이어토닉 7코드 버튼(`I`~`VII`)이나 단축키(`1`~`7`)를 누르면 해당 마디에 선택된 코드가 할당됨
  - 마디가 선택되지 않은 상태에서 팔레트를 클릭하면 첫 번째 빈 마디를 자동 선택하거나 "먼저 마디를 선택하세요" 안내 피드백 제공
- [x] 키보드 단축키 지원 (숫자 1~7로 선택된 마디에 다이어토닉 코드 빠른 입력)

### 코드 상세 속성 팝오버 / 드로어

- [x] 마디/코드 클릭 시 열리는 상세 속성 편집 UI:
  - 도수 선택: `I`, `II`, `III`, `IV`, `V`, `VI`, `VII` 및 임시표 `bIII`, `bVI`, `bVII`
  - 화음 유형 (quality): `major`, `minor`, `diminished`, `dominant` 등 카탈로그 옵션
  - 텐션/확장 (extension): none, `maj7`, `7`, `m7`, `9`
  - 베이스 도수 (bass_degree): 슬래시 코드 베이스 도수 선택 (예: `G/B`의 `VII`)
- [x] 전형적인 코드 변형 우선 추천 탭 (자주 쓰이는 세컨더리 도미넌트, 모달 인터체인지 코드 퀵 버튼)
- [x] [코드 삭제] 버튼 (선택한 마디/박의 코드를 비우기)
- [x] 실시간 실제 코드명 프리뷰 표시

완료 기준:
- [x] 클릭 한 번으로 기본 다이어토닉 코드가 입력된다.
- [x] 팝오버를 통해 복잡한 텐션이나 슬래시 코드도 직관적으로 조합하여 입력할 수 있다.

---

## Phase 6. 4마디 블록 추천 패널 UI

목표: 4마디 블록 단위로 시스템 진행 160선에서 적절한 진행을 찾아 차트에 원클릭 적용한다.

### 4마디 블록 컨트롤

- [x] 현재 구간의 4마디 블록 네비게이션:
  - 8마디 구간: [1~4마디 블록], [5~8마디 블록] 선택 탭
  - 7마디 구간: [1~4마디 블록]만 활성화, 5~7마디는 "추천 불가 (4마디 미만)" 배지 표시
  - 4마디 미만 구간: "4마디 이상 구간에서만 추천 가능" 안내 표시
- [x] 블록 상태 감지 및 예외 UI:
  - `multi_chord_excluded`: 블록 내에 한 마디 2개 이상 코드가 있으면 "복수 코드가 포함된 블록은 추천에서 제외됩니다" 안내
  - `all_filled`: 4마디가 모두 입력된 경우 "모든 코드가 입력되었습니다. 추천을 받으려면 일부 마디를 비워주세요" 안내
  - `no_match`: 조건에 맞는 진행이 없을 때 "조건에 맞는 추천 진행이 없습니다" 안내

### 추천 목록 뷰

- [x] 정렬 기준 선택 탭:
  - 대중성 우선 (`popularity`)
  - 코드 연결성 우선 (`connectivity`)
  - 다양성 우선 (`diversity`)
  - 무작위 (`random`)
- [x] `POST /api/recommendations` 비동기 요청 및 로딩 스켈레톤 UI
  - 사용자가 명시적인 텐션을 선택하지 않은 기본 도수 상태에서는 `extension: undefined`로 전달하여 `maj7`, `7` 등 상위 텐션 진행도 포괄 추천되도록 유연성 확보
- [x] 추천 진행 카드 컴포넌트 (한 번에 3개 표시):
  - 진행 이름 (예: `I - V - VI - IV (팝 4코드 진행)`)
  - 설명 및 송폼 태그 (`Chorus`, `Intro` 등)
  - 4개 스텝 시각화: 도수(`I - V - VI - IV`) 및 현재 조성 실제 코드명(`C - G - Am - F`) 동시 표기
  - 다양성 그룹 배지
- [x] 페이지네이션 (이전/다음 페이지 버튼 및 페이지 번호)
- [x] 이전 페이지에 노출된 `diversityGroup`을 `excludeDiversityGroups`로 누적 전달

### 추천 적용 로직

- [x] 카드별 [이 진행 적용하기] 버튼
- [x] 부분 입력 보존 병합:
  - `bar.chords.length === 0`인 빈 마디에만 추천 스텝의 코드를 `beat: 1`로 주입하고, 기존 입력 코드는 그대로 보존
- [x] 차트 업데이트 후 "추천 진행이 적용되었습니다" 피드백 제공

완료 기준:
- [x] 4마디 블록 규칙에 맞게 추천 대상이 필터링된다.
- [x] 추천 카드의 도수와 실제 코드명이 일치하며, 적용 버튼 클릭 시 기존 입력을 해치지 않고 빈 마디가 정확히 채워진다.

---

## Phase 7. 실시간 기법 분석 피드백 UI

목표: 사용자가 코드를 수정했을 때 음악 이론적 기법(모달 인터체인지 등)을 즉시 시각적으로 피드백한다.

- [x] 코드 변경 이벤트 감지 및 디바운스 처리
- [x] `POST /api/analysis` 호출 (변경 전 코드, 변경 후 코드, 현재 마디/박, 4마디 블록 컨텍스트 전달)
- [x] 복수 코드 마디 분석 트리거 필터:
  - 중간 박 코드 변경은 분석 요청 생략
  - 첫 코드 변경은 이전 마디 관계만 분석
  - 마지막 코드 변경은 다음 마디 관계만 분석
- [x] 코드 삭제(빈 마디화) 시 기법 분석 예외 처리:
  - 코드가 삭제되어 `chords: []`가 된 경우 API 호출을 생략하고 `lastTechnique`를 `null`로 리셋
- [x] 기법 분석 피드백 컴포넌트:
  - 기법이 감지된 경우: 우측 분석 패널 또는 코드 마디 상단에 기법 배지 표시 (예: `[모달 인터체인지]`, `[세컨더리 도미넌트]`)
  - 기법 설명 카드: "같은 으뜸음의 평행조에서 빌려온 서브도미넌트 마이너(iv) 진행입니다." 등 사용자 친화적 설명 렌더링
  - 일치하는 기법이 없거나(`technique: null`) 복수 코드 마디인 경우: 불필요한 영역 노출 없이 조용히 숨김 처리
- [x] 블록 경계(4마디 끝 ↔ 다음 블록 시작)에서는 분석이 트리거되지 않도록 클라이언트 사전 차단

완료 기준:
- [x] 다이어토닉 코드를 모달 인터체인지 코드(예: F → Fm)로 변경했을 때 즉시 기법 안내가 화면에 나타난다.
- [x] 복수 코드 마디이거나 규칙에 없는 코드일 때는 UI가 어색하지 않게 정리된다.

---

## Phase 8. 사용자 진행 관리 UI (시나리오 B)

목표: 시스템 추천과 분리된 사용자 전용 4마디 진행 저장 및 와일드카드 검색 화면을 제공한다.

### 화면 진입 및 분리

- [ ] 상단 내비게이션 또는 별도 탭으로 "사용자 진행 보관함" 메뉴 제공
- [ ] 시스템 추천 UI와 섞이지 않도록 독립된 모달/페이지로 구성

### 사용자 진행 등록 UI

- [x] 진행 등록 모달:
  - 진행 이름 입력 (필수)
  - 4개 스텝 도수 입력기 (1~4번 위치별 대문자 도수 및 기본 quality 설정)
  - 자주 쓰는 송폼 다중 선택 체크박스 (`Intro`, `Verse`, `Chorus` 등)
  - 설명 및 메모 텍스트에어리어
- [x] `POST /api/user-progressions` 호출 및 등록 완료 처리

### 사용자 진행 검색 및 목록 UI

- [x] 이름 검색 입력창
- [x] **4자리 도수 패턴 검색기**:
  - 4개의 입력 인풋 박스 (예: `[ x ] [ II ] [ I ] [ x ]`)
  - `x`를 와일드카드로 입력할 수 있는 안내 가이드 문구
  - 4자리 모두 `x`인 경우 검색 버튼 비활성화 및 경고 표시
- [x] `POST /api/user-progressions/search` 연동 및 결과 카드 렌더링
- [x] 사용자 진행 상세 보기 및 삭제 버튼 (`DELETE /api/user-progressions/:id`)
- [x] 검색 결과 도수·코드 속성을 현재 프로젝트 `tonic` 기준 실제 코드명으로 클라이언트 변환

완료 기준:
- [x] 사용자 진행이 정상 등록되고 시스템 추천 결과에는 노출되지 않는다.
- [x] `x`를 포함한 4자리 패턴 검색이 백엔드 결과와 정확히 일치하여 렌더링된다.

---

## Phase 9. 클라이언트 통합 워크플로우 & 사용성 검증

목표: 초보자가 10분 안에 첫 곡을 완성할 수 있도록 전체 UX를 매끄럽게 다듬고 오류를 방어한다.

- [x] 사용자 안내 온보딩 툴팁 / 빠른 시작 가이드:
  - "1. 송폼을 추가하세요" → "2. 다이어토닉 코드를 찍거나 추천을 받으세요" → "3. 저장하세요"
- [x] 키보드 내비게이션 및 접근성:
  - 좌/우 화살표로 마디 간 포커스 이동
  - Space/Enter로 코드 팝오버 열기
  - Esc로 팝오버/모달 닫기
- [x] 네트워크 에러 복구 및 재시도 UX
- [x] 전체 사용자 시나리오 E2E 테스트 검증:
  - **시나리오 A**: 빈 프로젝트 생성 → C Major 선택 → Verse 8마디 / Chorus 8마디 추가 → 추천 적용으로 16마디 완성 → 수동 저장
  - **시나리오 B**: 사용자 진행 탭 이동 → `x - II - I - x` 검색 → 결과 확인
  - **시나리오 C**: 코드 차트에서 특정 코드를 변형하고 기법 분석 설명 확인
- [x] 입문자 대상 10분 완성 플로우 자체 시뮬레이션 및 시간 측정

완료 기준:
- [x] 모든 사용자 시나리오 흐름에서 콘솔 에러나 UI 멈춤 없이 쾌적하게 동작한다.
- [x] 빈 프로젝트에서 16마디 곡 완성 및 저장까지 10분 이내에 완료할 수 있다.

---

## Phase 10. 프론트엔드 완료 기준

- [x] 모든 P0 화면(프로젝트 목록, 송폼 편집기, 코드 차트, 추천 패널, 기법 분석, 사용자 진행)이 구현된다.
- [x] 모든 편집 상태는 클라이언트 초안으로 안전하게 보존되며 명시적 저장 시에만 서버에 반영된다.
- [x] 12개 장조 조성 변환이 화면에서 오차 없이 즉각 반영된다.
- [x] 4마디 추천 및 적용이 비파괴적으로 동작하며 빈 마디를 채운다.
- [x] 코드 변경 시 기법 분석 피드백이 실시간으로 제공된다.
- [x] 사용자 진행 검색이 시스템 추천과 분리되어 와일드카드와 함께 작동한다.
- [x] 브라우저 환경에서 입문자가 10분 안에 한 곡을 완성할 수 있는 직관적인 UI가 완성된다.

---

## 후속 작업 (Post-MVP)

- [ ] Web Audio API 기반 코드 화음 피아노/기타 사운드 재생
- [ ] BPM 메트로놈 및 마디 진행 바 재생 컨트롤러
- [ ] 다크 모드 테마 지원
- [ ] 반응형 모바일/태블릿 터치 UI 최적화
- [ ] Electron 또는 Tauri를 이용한 설치형 데스크톱 앱 패키징
- [ ] 단조(Minor Key) 독립 조성 선택 지원
