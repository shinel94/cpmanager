# Chord Progression Manager 제품 기획서

## 1. 문서 개요

- 제품명: Chord Progression Manager
- 제품 유형: 브라우저 기반 로컬 작곡 보조 프로그램
- 대상: 작곡 입문자 및 취미 작곡가
- 문서 목적: 제품 구조와 사용자 경험 정의
- 초기 실행 방식: Node.js가 설치된 PC에서 로컬 서버 실행 후 브라우저 접속
- 데이터 저장: SQLite

## 2. 제품 목표

사용자가 송폼과 조성을 설정하고, 도수 기반의 4마디 코드 진행을 추천받아 빠르게 한 곡의 코드 진행을 완성할 수 있도록 한다.

## 3. 제품 원칙

- 추천은 4마디·4코드 단위로 단순하게 제공한다.
- 내부 데이터는 도수와 코드 속성을 보존하고, 화면에는 선택한 조성의 실제 코드로 표시한다.
- 사용자의 선택과 수정 결과를 우선한다.
- 코드 변경의 음악 이론적 의미는 규칙 기반으로 설명한다.
- 사용자가 저장한 진행은 기본 추천과 분리한다.
- 초기 버전은 기능 범위를 제한해 핵심 작곡 흐름을 검증한다.

## 4. 사용자 시나리오

### 시나리오 A: 새 곡 작성

1. 사용자가 새 프로젝트를 만든다.
2. 곡 이름을 입력한다.
3. C Major와 같은 조성을 선택한다.
4. Intro, Verse, Pre-Chorus, Chorus 등의 송폼을 추가한다.
5. 각 구간의 마디 수를 설정한다.
6. 코드 차트에서 특정 마디의 도수 또는 코드를 선택한다.
7. 조건에 맞는 4마디 추천 목록을 확인한다.
8. 추천 결과를 선택하면 실제 코드로 변환되어 차트에 반영된다.
9. 필요한 코드를 변경하고 변경 기법을 확인한다.
10. 곡을 저장한다.

### 시나리오 B: 사용자 진행 검색

1. 사용자가 사용자 진행 관리 화면을 연다.
2. 코드 진행을 도수 4개로 입력한다.
3. 진행 이름, 자주 사용된 송폼, 설명을 입력한다.
4. 저장된 진행은 일반 추천에는 포함되지 않는다.
5. 검색 화면에서 4개 위치와 `x` 와일드카드를 사용해 검색한다.
6. 검색 결과로 조건에 맞는 4마디 진행만 확인한다.

### 시나리오 C: 코드 변경 기법 확인

1. 사용자가 코드 차트의 코드를 선택한다.
2. 다른 코드 또는 코드 속성을 선택한다.
3. 시스템이 변경 전후의 4마디 범위와 인접 코드 관계를 분석한다.
4. 규칙에 맞는 기법이 있으면 기법명과 간단한 설명을 표시한다.
5. 여러 기법이 일치하면 DB의 priority가 높은 기법 하나를 표시한다.

## 5. 제품 범위

### MVP 포함

- 프로젝트 생성·저장·불러오기·삭제
- 송폼 편집
- 장조 조성 선택
- 4/4박자 기반 코드 차트
- 1마디 1코드 입력
- 1박 단위 코드 입력
- 4마디·4코드 진행 추천
- 도수와 코드 속성 저장
- 조성에 따른 실제 코드 변환
- 추천 기준 선택
- 코드 기법 판별
- 기본 추천 진행 데이터베이스 제공
- 사용자 진행 등록·검색·관리

### MVP 제외

- 단조를 독립적인 곡 조성으로 선택
- PDF·이미지·MIDI 내보내기
- MIDI 또는 오디오 재생
- 자동 저장
- 계정·온라인 서버·협업
- 4마디를 넘는 기법 분석
- 여러 코드가 포함된 마디의 자체 분석

## 6. 핵심 도메인 규칙

### 조성

- 초기 곡 조성은 장조만 선택할 수 있다.
- A minor를 사용하려는 경우 C Major 조성 안에서 상대 단조 코드를 구성하는 방식으로 처리한다.
- 조성은 표시용 실제 코드 변환과 도수 해석의 기준이 된다.

### 송폼

- 기본 제공 구간명은 Intro, Verse, Pre-Chorus, Chorus, Interlude, Bridge, Outro이다.
- 사용자가 직접 구간명을 입력할 수 있다.
- 구간 순서에는 제한이 없다.
- 각 구간은 정수 단위의 마디 수를 가진다.

### 코드 입력

- 기본 박자는 4/4이다.
- 기본 입력은 1마디 1코드이다.
- 필요한 경우 한 마디의 1~4박에 각각 코드를 입력할 수 있다.
- 여러 코드가 있는 마디는 해당 마디 자체의 추천과 기법 분석에서 제외한다.

### 추천 단위

- 추천 단위는 4마디·4코드이다.
- 8마디 구간은 1~4마디와 5~8마디를 각각 분석한다.
- 7마디 구간은 1~4마디만 추천하고 5~7마디는 추천하지 않는다.
- 4마디보다 짧은 구간은 추천 대상에서 제외한다.
- 3~6마디처럼 임의로 겹치는 창은 사용하지 않는다.

### 기법 분석

- 분석 범위는 최대 4마디이다.
- 4마디 블록의 마지막 코드와 다음 블록 첫 코드 사이에는 분석하지 않는다.
- 여러 코드가 있는 마디는 해당 마디 자체를 분석하지 않는다.
- 이전 마디 분석에 필요한 경우 해당 마디의 마지막 코드를 사용한다.
- 다음 마디 분석에 필요한 경우 해당 마디의 첫 코드를 사용한다.
- 매칭되는 기법이 여러 개이면 priority가 높은 하나를 표시한다.

## 7. 코드 데이터 표현

코드는 도수만 저장하지 않고 다음 속성을 함께 저장한다.

- 기능 도수: `I`, `ii`, `V`, `bVI` 등
- 화음 유형: major, minor, diminished 등
- 확장: maj7, 7, m7, 9 등
- 베이스: 기본음 또는 슬래시 코드의 베이스 도수
- 차용·변형 여부

예시:

```text
degree: V
quality: dominant
extension: 7
bass_degree: null
```

내부 저장값을 기준으로 조성에 맞는 실제 코드명을 생성한다.

## 8. 초기 코드 범위

- 장조 코드
- 단조 코드
- 디미니시드 코드
- `maj7`
- `7`
- `m7`
- `9`
- 대표적인 모달 인터체인지
- 세컨더리 도미넌트
- 슬래시 코드

화면에서는 전형적으로 많이 사용하는 코드 변형을 우선 노출하고, 상세 선택 영역에서 추가 변형을 선택할 수 있도록 한다.

## 9. 추천 기능

### 입력 조건

- 선택한 송폼 구간
- 4마디 블록 위치
- 조성
- 이미 입력된 도수 또는 코드
- 앞뒤 코드 관계
- 추천 정렬 기준

### 추천 기준

- 무작위
- 대중성 우선
- 코드 연결성 우선
- 다양성 우선

추천 데이터에는 내부 수작업 기준과 priority를 저장한다. 무작위 기준은 조건에 맞는 데이터 중에서 선택한다.

### 일부 코드가 입력된 경우

- 기존 입력 코드는 유지한다.
- 저장된 4마디 진행과 입력된 위치가 일치하는 경우만 결과에 포함한다.
- 일치하는 진행이 없으면 추천 없음으로 표시한다.
- 4마디가 모두 입력된 경우에는 추천하지 않는다.

## 10. 사용자 진행 검색

- 사용자 진행은 4개의 도수 위치로 저장한다.
- 검색은 실제 코드명이 아니라 도수 패턴을 기준으로 한다.
- 검색 입력은 정확히 4개 위치를 가진다.
- `x`는 해당 위치의 임의 값을 의미한다.
- 모든 위치가 `x`인 검색은 실행하지 않는다.
- 조건을 충족하는 4마디 진행만 결과에 포함한다.
- 사용자 진행은 일반 추천 결과에 포함하지 않는다.
- 검색 화면에서는 이름 검색과 도수 패턴 검색을 제공한다.

예시:

```text
저장 진행: I - ii - I - I
검색:       x - ii - I - x  -> 일치
검색:       I - ii - x - x  -> 일치
검색:       x - x - I - I    -> 일치
검색:       I - I - x - x    -> 불일치
검색:       x - x - x - x    -> 실행하지 않음
```

## 11. 화면 구성

### 프로젝트 목록

- 새 곡 만들기
- 저장된 곡 목록 보기
- 곡 이름으로 검색
- 곡 불러오기
- 곡 삭제

### 송폼 편집

- 구간 추가
- 구간명 입력 또는 선택
- 구간 순서 변경
- 마디 수 설정
- 구간 삭제

### 조성 선택

- 장조 조성 선택
- 현재 조성 표시
- 조성에 따른 코드명 변환

### 코드 차트 편집

- 송폼 구간과 마디 표시
- 도수 입력
- 다이어토닉 코드 목록 표시
- 코드 변형 선택
- 1박 단위 코드 입력
- 코드 삭제·변경

### 4마디 추천 목록

- 추천 진행 3개 표시
- 페이지 이동
- 추천 기준 선택
- 도수 진행 표시
- 실제 코드 표시
- 추천 적용
- 추천 없음 상태 표시

### 기법 분석 결과

- 변경한 코드 표시
- 적용된 기법명 표시
- 기법 설명 표시
- 일치하는 기법이 없으면 결과를 표시하지 않음

### 사용자 진행 관리

- 진행 이름 입력
- 4개 도수 입력
- 자주 사용된 송폼 복수 선택
- 설명·메모 입력
- 진행 저장
- 이름 검색
- 도수 패턴 검색
- 진행 삭제

## 12. 저장 정책

- 저장은 사용자가 명시적으로 실행한다.
- 자동 저장은 MVP에서 제공하지 않는다.
- 프로젝트 데이터는 SQLite에 저장한다.
- PDF·이미지·MIDI 파일로 내보내지 않는다.
- 프로젝트와 추천 데이터는 테이블을 분리한다.

## 13. 성공 기준

설명을 들은 작곡 입문자가 빈 프로젝트에서 전형적인 송폼을 만들고 첫 코드부터 마지막 코드까지 10분 이내에 저장할 수 있어야 한다.

## 14. 향후 확장

- BPM 설정
- 코드 진행 MIDI 재생
- 기본 MIDI 파일 기반 코드 소리 재생
- 설치형 데스크톱 배포
- 추천 데이터 확장
- 단조 조성 독립 지원
